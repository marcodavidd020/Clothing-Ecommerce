import 'package:flutter/material.dart';

/// Constantes de UI específicas para el módulo de autenticación
class AuthUI {
  // Espaciados
  static const double verticalSpacing = 16.0;
  static const double smallVerticalSpacing = 8.0;
  static const double largeVerticalSpacing = 24.0;
  static const double horizontalPadding = 24.0;

  // Dimensiones
  static const double buttonHeight = 48.0;
  static const double buttonRadius = 100.0;
  static const double inputRadius = 8.0;
  static const double socialButtonIconSize = 24.0;

  // Animación
  static const Duration animationDuration = Duration(milliseconds: 300);

  // Colores
  static const Color errorColor = Colors.red;

  // Validación
  static const int minPasswordLength = 6;
}
